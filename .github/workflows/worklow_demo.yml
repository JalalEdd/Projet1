name: DémoMémoire
on: push
jobs:
  Clean: 
    name: Clean application
    runs-on: windows-latest
    steps:
      - name: Cleanup
        run: dotnet clean 
  Build:
    name: Build application
    runs-on: windows-latest
    needs: [Clean]
    steps:
      - name: Build
        run: dotnet build    
  Test:
    name: Test application
    runs-on: windows-latest
    needs: [Build]
    steps:
      - name: Test
        run: dotnet test
      
        
